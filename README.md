# OrBIT-KDF
OrBIT is an experimental key derivation function that uses chaotic fractal dynamics plus a cryptographic hash to turn user credentials into high‑entropy keys. It’s a prototype password KDF focused on hash‑like output and tunable cost per evaluation.
